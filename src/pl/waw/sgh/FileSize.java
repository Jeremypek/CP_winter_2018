package pl.waw.sgh;

import java.io.File;

public class FileSize {
/*
    public static void main(String[] args) {
        File newFile = new File("c:/temp");

        if (newFile.isDirectory()) {
            File[] files = newFile.listFiles();
            for (File f : files) {

                System.out.println(f.getName() + " " + f.length());
            }
        }
    }
}


public static long CheckDirectorySize(File newFile) { //newFile: "C:\temp"
    long abc=0;
    if (newFile.isDirectory()) {
        File[] files = newFile.listFiles();
        System.out.println("W folderze " + newFile.getName()+" sa " + files.length + " plik");
        for (File f : files) {
            if(f.isFile()) {
                System.out.println("Plik " + f.getName() + " rozmiar:" + f.length());
                abc += f.length();
            } else if (f.isDirectory()) {
                long subfolderSize = CheckDirectorySize(f);
                System.out.println("Podfolder " + f.getName() + " size: " +subfolderSize);
                abc += subfolderSize;
            }
        }

    }
    return abc;
}
public

*/
}
