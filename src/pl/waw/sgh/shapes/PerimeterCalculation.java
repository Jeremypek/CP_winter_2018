package pl.waw.sgh.shapes;

//interface does not have any implementations

public interface PerimeterCalculation {

    //we do not need to specify the visibility (public private double) - it need to be public anyway
    double calculatePerimeter();
}
